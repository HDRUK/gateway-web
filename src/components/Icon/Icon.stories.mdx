import { Meta, Story, Canvas } from '@storybook/addon-docs';
import SVG from 'react-inlinesvg';
import starIcon from '../../images/star.svg';

import Icon from './Icon';

<Meta
    title='Core/Icon'
    component={Icon}
    args={{
        ...Icon.defaultProps,
    }}
    argTypes={{
        color: {
            control: 'text',
        },
    }}
/>

<Canvas>
    <Story name='Sizes'>
        {props => {
            return ['xxs', 'xs', 'sm', 'md', 'lg', 'xl', '2xl', '3xl', '4xl', '5xl'].map(size => (
                <Icon svg={<SVG src={starIcon} />} {...props} size={size} mr={2} />
            ));
        }}
    </Story>
</Canvas>
