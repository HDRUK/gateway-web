import { Meta, Story, Canvas } from '@storybook/addon-docs';

<Meta title='Hooks/useDebounce' />

```js
const debouncedValue = useDebounce('inputString', 500);

React.useEffect(() => {}, [debouncedValue]);
```
