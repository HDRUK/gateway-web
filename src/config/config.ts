const config = {
    JWT_COOKIE: "token",
    ENTITY_ACTION_COOKIE: {
        COOKIE_NAME: "gw-search-entity-action",
        ACTION_ADD_LIBRARY: "action-add-library",
    },
};

export default config;
