const BASE_METADATA_PATH = "latest_metadata.metadata.metadata";

const TITLE_LOCATION = `${BASE_METADATA_PATH}.summary.title`;
const PUBLISHER_NAME_LOCATION = `${BASE_METADATA_PATH}.summary.summary.publisher.publisherName`;

export { TITLE_LOCATION, PUBLISHER_NAME_LOCATION };
